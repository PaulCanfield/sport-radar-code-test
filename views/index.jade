extends layout

block content
  div(class="container")
    table(class="table table-striped table-bordered")
      thead
        tr
          th Team
          th Nickname
          th Bye Week
      tbody
        each team, index in teams
          tr
            td #{team.FullName}
            td #{team.Nick}
            td #{team.getByeWeek()}